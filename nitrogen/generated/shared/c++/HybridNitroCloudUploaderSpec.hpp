///
/// HybridNitroCloudUploaderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `UploadResult` to properly resolve imports.
namespace margelo::nitro::nitroclouduploader { struct UploadResult; }
// Forward declaration of `UploadState` to properly resolve imports.
namespace margelo::nitro::nitroclouduploader { struct UploadState; }
// Forward declaration of `UploadProgressEvent` to properly resolve imports.
namespace margelo::nitro::nitroclouduploader { struct UploadProgressEvent; }

#include "UploadResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>
#include <optional>
#include "UploadState.hpp"
#include "UploadProgressEvent.hpp"
#include <functional>

namespace margelo::nitro::nitroclouduploader {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroCloudUploader`
   * Inherit this class to create instances of `HybridNitroCloudUploaderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroCloudUploader: public HybridNitroCloudUploaderSpec {
   * public:
   *   HybridNitroCloudUploader(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroCloudUploaderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroCloudUploaderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroCloudUploaderSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<UploadResult>> startUpload(const std::string& uploadId, const std::string& filePath, const std::vector<std::string>& uploadUrls, std::optional<double> maxParallel, std::optional<bool> showNotification) = 0;
      virtual std::shared_ptr<Promise<void>> pauseUpload(const std::string& uploadId) = 0;
      virtual std::shared_ptr<Promise<void>> resumeUpload(const std::string& uploadId) = 0;
      virtual std::shared_ptr<Promise<void>> cancelUpload(const std::string& uploadId) = 0;
      virtual std::shared_ptr<Promise<UploadState>> getUploadState(const std::string& uploadId) = 0;
      virtual void addListener(const std::string& eventType, const std::function<void(const UploadProgressEvent& /* event */)>& callback) = 0;
      virtual void removeListener(const std::string& eventType) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroCloudUploader";
  };

} // namespace margelo::nitro::nitroclouduploader
